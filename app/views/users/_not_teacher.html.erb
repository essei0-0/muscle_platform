<%= form_with(model: user.passive_deep_relationships.find_by(student_id: current_user.id), html: { method: :delete }) do |f| %>
  <%= f.submit "弟子をやめる", class: "btn btn-primary",
  data: { confirm: "弟子をやめます。よろしいですか？"} %>
<% end %>
