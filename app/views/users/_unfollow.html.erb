<%= form_with(model: user.passive_relationships.find_by(follower_id: current_user.id), html: { method: :delete }) do |f| %>
  <%= f.submit "フォロー中", class: "btn btn-primary",
  data: { confirm: "フォローを解除します。よろしいですか？"} %>
<% end %>
